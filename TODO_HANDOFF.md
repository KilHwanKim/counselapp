# Next LLM Handoff

## Current Status
- 이전에 검토하던 기능 변경은 원래대로 되돌린 상태입니다.
- 현재 작업 트리 기준으로 코드 변경은 없고, 남아 있는 변경 파일은 이 문서 `TODO_HANDOFF.md` 하나뿐입니다.
- 따라서 다음 LLM은 "기존 변경 이어받기"가 아니라 "무엇을 새로 할지 다시 정리"하는 시점에서 시작하면 됩니다.

## What Is Actually Changed Now
- `TODO_HANDOFF.md`: 현재 시점 기준 TODO와 작업 방향을 정리한 문서.

## What Looks Stable
- 저장소의 앱 코드는 현재 마지막 커밋 기준 상태로 돌아가 있습니다.
- 이전에 논의된 공휴일 관리, 실제 수업 상태 관리, DDL 동기화 확장 등은 현재 작업 트리에 반영되어 있지 않습니다.

## What Should Be Done Next
1. 우선 작업 목표를 다시 확정
- 다음에 만들 기능이 무엇인지 먼저 한 줄로 다시 정해야 합니다.
- 후보 예시: 공휴일 관리, 실제 수업 취소/복구, DB 시드 정리, DDL 동기화 개선.

2. 범위부터 작게 고정
- 한 번에 UI, API, SQL을 모두 바꾸기보다 한 기능 단위로 자르는 것이 안전합니다.
- 추천은 `하나의 사용자 흐름` 기준으로 자르는 것입니다.

3. 구현 전 확인할 것
- 어떤 화면에서 시작할지 (`index.html`인지 별도 화면인지)
- DB 스키마 변경이 필요한지
- 로컬 전용인지 dev DB 반영까지 필요한지

4. 작업 시작 순서 추천
- 요구사항 확정
- 관련 파일 확인
- 필요한 DB/API/UI 범위만 최소 변경
- 수동 테스트 시나리오 정리
- 그 다음 커밋

## Recommended Next TODO
- [ ] 다음에 만들 기능 1개를 확정한다.
- [ ] 그 기능에 필요한 파일 범위를 3개 이내로 먼저 좁힌다.
- [ ] DB 변경이 필요한지 먼저 판단한다.
- [ ] DB 변경이 필요하면 SQL 파일과 적용 절차를 같이 정한다.
- [ ] UI와 API를 동시에 바꿀지, API부터 만들지 결정한다.
- [ ] 구현 후 최소 수동 테스트 체크리스트를 만든다.

## Open Questions
- 다음 작업은 기능 추가인가, 버그 수정인가, DB 정리인가.
- 변경 범위를 루트 `index.html` 중심으로 잡을지, 별도 파일로 분리할지.
- dev DB까지 바로 반영할지, 로컬에서만 먼저 검증할지.

## Risks
- 목표를 다시 정하지 않고 바로 구현에 들어가면 이전처럼 범위가 커질 수 있습니다.
- UI, API, SQL을 동시에 건드리면 검증 포인트가 많아져 중간에 되돌리기 쉬워집니다.
- DDL이나 시드 작업은 로컬과 dev 반영 범위를 먼저 정하지 않으면 혼선이 생길 수 있습니다.

## Suggested Starting Point For Next LLM
- 먼저 사용자에게 "이번에 정확히 어떤 기능을 먼저 만들지" 한 가지를 확인받고 시작하세요.
- 기능이 정해지면 관련 파일만 다시 읽고, 작은 범위의 실행 계획부터 세우는 것이 좋습니다.
